name: Build and Release Worker

on:
  push:
    tags:
      - 'v*' 

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write 

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm install

      - name: Build Worker
        run: npx wrangler deploy --dry-run --outdir dist

      - name: Rename Output
        run: mv dist/index.js dist/worker.js

      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: dist/worker.js
          generate_release_notes: true 
          draft: false
          prerelease: false
          body: |
            ## 📦 懒人安装法 (单文件部署)
            
            1. 下载 `worker.js`。
            2. 在 Cloudflare Dashboard 创建一个新的 Worker。
            3. 点击 "Edit Code"，将 `worker.js` 的内容全部粘贴进去，点击 "Deploy"。
            4. **关键步骤**：去 Worker 的 **Settings -> Variables** 页面，手动添加以下变量：
               - `ENABLE_AUTH`: (明文) true
               - `SERVER_COOKIES`: (加密/Secret) 你的 Cookie JSON 数组，比如 ["BDUSS=xxx"]
               - `SESSION_SECRET`: (加密/Secret) 随机字符串
               - `LINUX_DO_CLIENT_ID`: (加密/Secret) 你的 Client ID
               - `LINUX_DO_CLIENT_SECRET`: (加密/Secret) 你的 Client Secret
            5. 去 **Settings -> Triggers** 添加 Cron Trigger（比如 `0 */2 * * *`）。
      
      - name: Test Build (Manual Trigger Only)
        if: github.event_name == 'workflow_dispatch'
        run: echo "Manual build successful! Worker.js generated in dist/"